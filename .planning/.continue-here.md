---
phase: post-e2e-deferred-cleanup
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-03-04T03:41:47.559Z
---

<current_state>
Executing the post-E2E deferred cleanup plan from `docs/plans/2026-03-03-post-e2e-deferred.md`.
Task 1 (legacy discovery.run removal) is COMPLETE. Task 2 (deprecated contract fields) is in progress but no edits started yet. Tasks 3 and 4 are pending.
</current_state>

<completed_work>

- Task 1: Remove legacy discovery.run.job.ts and all references - DONE
  - Deleted `apps/worker/src/jobs/discovery.run.job.ts` (~1713 LOC)
  - Deleted `apps/api/test/integration/discovery.run.integration.test.ts`
  - Deleted `apps/worker/test/integration/pipeline-domain-persistence.integration.test.ts`
  - Removed import + queue registration from `apps/worker/src/queues.ts`
  - Removed import, handler registration, legacy provider log, `discoveryProviderOrder`, and `DiscoveryProvider` contracts import from `apps/worker/src/index.ts`
  - Removed `boss.createQueue('discovery.run')` from `apps/api/src/index.ts`
  - Simplified `enqueueDiscoveryRun` in `apps/api/src/index.ts` — always routes to `discovery.seed`, removed legacy fallback branch
  - Updated `pipeline-full-chain.test.ts` — removed discovery.run import/stage 1, replaced with direct DB seeding of lead + discovery record, removed unused Apollo/Brave/GooglePlaces/LinkedIn/CompanySearch adapter factories
  - Updated comment in `pipeline-e2e.test.ts`
</completed_work>

<remaining_work>

- Task 2: Remove deprecated `provider`/`cursor` fields from contract schema — IN PROGRESS (no edits yet)
  - Remove `provider` and `cursor` from `CreateDiscoveryRunRequestSchema` in `packages/contracts/src/discovery.contract.ts`
  - Remove `provider` and `cursor` from `DiscoveryRunJobPayload` Pick in `apps/api/src/modules/discovery/discovery.service.ts`
  - Remove `provider: input.provider` and `cursor: input.cursor` from payload construction in same file
  - Web UI already does NOT send these fields — safe to remove
  - NOTE: `provider` in LeadDiscoveryRecord (DB column) is DIFFERENT — that stays. Only removing request-level deprecated fields.

- Task 3: Replace console.log with structured logger — NOT STARTED
  - `apps/api/src/modules/messaging/messaging.service.ts` lines 121, 149 — `console.error` (service has no logger import, needs injection or direct import)
  - `apps/worker/src/index.ts` line ~908 — `console.error('Worker boot failed:')` (logger not yet initialized at this point — consider keeping or wrapping)
  - `apps/api/src/index.ts` line ~618 — `console.error('API boot failed:')` (same issue — logger not initialized)

- Task 4: Run full verification — NOT STARTED
  - `pnpm typecheck && pnpm lint && pnpm test && pnpm build`
</remaining_work>

<decisions_made>

- Chose Option A (full removal) for discovery.run — the schema already requires `countries` with `.min(1)`, so the legacy fallback path was dead code
- Kept `DISCOVERY_RUN_JOB_TYPE = 'discovery.run'` in `discovery.repository.ts` — this is for querying historical JobExecution records, not tied to the pg-boss queue
- Kept `DISCOVERY_RUN` in dispatcher.ts — confusingly named but it's the v2 task runner (calls `runSearchTask()`), NOT the legacy job
- Kept `triggerDiscoveryRun()` in web frontend — also the v2 admin task runner, not the legacy job
- Kept `DISCOVERY_RUN_MAX_TASKS` env var — used by both legacy (now removed) and v2 `run_search_task`
- Deleted pipeline-domain-persistence integration test — entirely depended on v1 discovery.run handler
- Replaced pipeline-full-chain stage 1 with direct DB seeding rather than deleting the entire test file (preserves stages 2-6 which test enrichment→messaging chain)
</decisions_made>

<blockers>
- None
</blockers>

<context>
The deferred plan has 5 items but only 4 are actionable (item 5 is "no action" for stub TODOs).
Verification has NOT been run yet — that's task 4. Expect possible type errors from the test file changes.
The `console.error` in boot handlers (task 3) is tricky because the logger isn't initialized yet at that point.
The messaging.service.ts console.error calls need a logger injected — the service currently has no logger dependency.
</context>

<next_action>
Start with Task 2: Edit `packages/contracts/src/discovery.contract.ts` to remove `provider` and `cursor` fields from `CreateDiscoveryRunRequestSchema`. Then update `apps/api/src/modules/discovery/discovery.service.ts` to remove those fields from `DiscoveryRunJobPayload` and the payload construction. Then move to Task 3 (console.log cleanup), then Task 4 (verification).
</next_action>
