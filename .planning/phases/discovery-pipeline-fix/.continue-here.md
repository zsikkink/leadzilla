---
phase: discovery-pipeline-fix
task: 4
total_tasks: 4
status: completed_pending_verification
last_updated: 2026-03-04T06:08:24.237Z
---

<current_state>
All 4 fix tasks are code-complete. Typecheck, lint, test, build all pass. Stuck pg-boss jobs
and search tasks cleaned up in Supabase DB. Worker needs restart (tsx watch should auto-reload)
and a NEW discovery run triggered from the UI to verify the fixes end-to-end.
</current_state>

<completed_work>

- Task 1: Fix SerpAPI "out of searches" (429) treated as retryable - DONE
  - `packages/discovery/src/providers/serpapi.client.ts`: Added `isTerminalError()` function,
    detect "run out of searches" on 429 as terminal (no retry). Added terminal error check in
    catch block to prevent retry loop. Reduced DEFAULT_MAX_ATTEMPTS 5->3, DEFAULT_BACKOFF_BASE_SECONDS 30->5.
- Task 2: Fix FallbackDiscoveryProvider quota detection - DONE
  - `packages/discovery/src/providers/fallback.provider.ts`: Added "run out of searches" and
    403 to `isQuotaExhaustedError()` detection. Provider was already wired correctly in worker.
- Task 3: Fix maxTasks mapping (lead count -> search tasks) - DONE
  - `apps/api/src/index.ts` line 361: Changed `maxTasks: payload.limit` to
    `maxTasks: Math.max(payload.limit * 3, 15)`. So "5 leads" -> 15 tasks, "25 leads" -> 75 tasks.
  - `packages/discovery/src/config.ts`: Default maxTasks raised from 40 to 75.
- Task 4: Database cleanup - DONE
  - Cancelled 2 stuck pg-boss discovery jobs
  - Reset 5 search tasks from RUNNING back to PENDING
  - Verified Google Places API key works (HTTP 200)
</completed_work>

<remaining_work>

- Restart worker (or confirm tsx watch picked up changes)
- Trigger a NEW discovery run from the UI (Start Discovery button)
- Verify: SerpAPI fails immediately -> falls back to Google Places -> returns results
- Verify: Discovery run transitions running -> completed with businesses found
- Verify: Progress counter updates in UI (X/Y processed)
- Commit all changes if E2E works
</remaining_work>

<decisions_made>

- SerpAPI 429 "out of searches" is now terminal, not retryable. Regular 429 (rate limit) still retries.
- Backoff reduced from 30s base to 5s base, max attempts from 5 to 3. Old config: ~10 min retry cycle.
  New config: ~30s retry cycle. Fails fast, lets fallback provider take over.
- maxTasks = max(leadCount * 3, 15) rather than = leadCount. Rationale: each query finds 5-20 businesses,
  so 3x over-generation gives enough candidates for scoring/filtering to hit the target lead count.
- Fisher-Yates sampling (already in working tree from prior session) stays as-is. When generateTasksV2
  produces more tasks than maxTasks, random sample down instead of throwing.
</decisions_made>

<blockers>
- SerpAPI account has ZERO searches remaining. All discovery relies on Google Places fallback until
  the SerpAPI account is topped up or a new key is configured.
- Google Places API has $200/month free credit. Should be sufficient for development/demo.
- ICP profile ID mismatch: pg-boss jobs reference `cmm2i3wl70000rkpx00kw2v08` which was NOT found
  in Supabase `IcpProfile` table query (only active profiles were returned). May indicate the profile
  was deactivated or the frontend is using a stale reference. Worth investigating if runs still fail.
</blockers>

<context>
User has never gotten a full discovery run to complete successfully. Three attempts all failed:
1. "0 tasks" - ICP industry mapping returned empty categories
2. "20520 tasks exceeds limit" - combinatorial explosion (18 countries x 40 cities x 19 categories x 3 templates x 2 types)
3. "stuck 15 min" - SerpAPI quota exhausted, 429 classified as transient, 10-min retry loop

Root cause analysis used systematic debugging: read all pipeline files, queried both databases
(Docker :5434 and Supabase :54322), checked pg-boss job states, tested SerpAPI key with curl.

Key insight: the FallbackDiscoveryProvider was already wired correctly in worker/src/index.ts
(the user was right that they set this up). The bug was in error classification — the fallback
never triggered because SerpAPI's 429 "out of searches" was retried 5 times before throwing,
and the fallback's `isQuotaExhaustedError` didn't match the error message.
</context>

<next_action>
Start with: Restart `pnpm dev` and trigger a new discovery run from the UI. Watch worker logs
for "[FallbackDiscoveryProvider] SerpAPI quota exhausted — switching to Google Places" message.
Verify businesses are discovered and the run completes. Then commit all changes.

Files modified (unstaged):
- apps/api/src/index.ts (maxTasks mapping)
- packages/discovery/src/providers/serpapi.client.ts (terminal error detection, reduced backoff)
- packages/discovery/src/providers/fallback.provider.ts (quota detection)
- packages/discovery/src/config.ts (default maxTasks 40->75)
- packages/discovery/src/seed_tasks.ts (Fisher-Yates sampling - from prior session)
- packages/discovery/src/queries/seeds.ts (prior session changes)
- packages/discovery/src/queries/generate_tasks.ts (prior session changes)
- packages/discovery/src/queries/icp-category-map.ts (prior session changes)
- packages/discovery/src/providers/types.ts (prior session changes)
- packages/discovery/src/normalization/phone.ts (prior session changes)
- packages/discovery/src/providers/google-places.client.ts (prior session changes)
- apps/worker/src/index.ts (prior session changes)
- apps/worker/src/jobs/discovery.seed.job.ts (prior session changes)
</next_action>
