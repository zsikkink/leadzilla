---
phase: session-b-frontend
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-03-04T04:04:30Z
---

<current_state>
Session B: Business Intel & Leads Page Overhaul. Tasks B1-B3 complete, B4 (Replace Activity Timeline with Intelligence Gathered) is mid-implementation. The imports have been updated but the component replacement is not done yet. Build will NOT pass in current state.
</current_state>

<completed_work>

- Task B1: Add "Business Intel" tab on Leads page + business link on Lead Detail - DONE
  - `leads/page.tsx`: Added tab bar with "Leads" (active) and "Business Intel" (links to businesses page) between header and filters
  - `leads/[id]/page.tsx`: Added clickable business name in header (links to businesses page with `?selected=` param), stored businessId in state
- Task B2: Fix Business Intel scores to show lead's blended score - DONE
  - `businesses/page.tsx`: Added `leadBlendedScore` to BusinessRow, modified `fetchBusinesses` to do 2 follow-up queries (business_conversions → leads) to get blendedScore from enrichment_data._scoreInfo
  - ScoreBadge now shows lead score as percentage when available, "Not scored" when score is 0
  - Both BusinessCard and BusinessDetailPanel updated
- Task B3: Make website URLs and Instagram handles clickable - DONE
  - BusinessDetailPanel: website_domain wrapped in `<a>` with external link icon, instagram_handle links to instagram.com
  - Social links section: URLs now clickable with external link icons
  - Contact info: emails have `mailto:`, phones have `tel:`
  - Instagram analytics: business email/phone also clickable
  - Added back-to-leads link and `useSearchParams` for `?selected=` auto-select
</completed_work>

<remaining_work>

- Task B4: Replace Activity Timeline with Intelligence Gathered section - IN PROGRESS
  - DONE: Removed unused imports (Clock, Cpu, Database, Send, Pencil, Search, MessageSquare), added new imports (Instagram, Monitor, Shield)
  - TODO: Remove the entire ActivityTimeline component and related code (statusReached, STATUS_ORDER, TimelineEvent, buildTimeline, ActivityTimeline — lines ~277-506 in original)
  - TODO: Replace CompanyIntelligence component with comprehensive IntelligenceGathered that includes:
    - Contact info from website scrape (emails→mailto, phones→tel, addresses) — handle both `contactInfo.emails` and `scrape.emails` formats
    - Social media links as clickable anchors (not just platform name badges)
    - Instagram analytics (bio, verified, category, engagement rate, media count)
    - Tech stack — handle both `technologies` (adapter output) and `techStack` (old format)
    - Certifications — handle both `businessSignals.certifications` and `scrape.certifications`
    - Decision makers with name + title
  - TODO: In the render section, remove the Activity Timeline card and ensure CompanyIntelligence/IntelligenceGathered card is comprehensive
- Task B5: Run verification (pnpm typecheck && pnpm lint && pnpm build) - NOT STARTED
</remaining_work>

<decisions_made>

- Used 3-query approach for B2 scores (businesses → business_conversions → leads) instead of nested PostgREST join for reliability
- ScoreBadge derives band from score value (>=0.7 HIGH, >=0.4 MEDIUM, else LOW) rather than storing it separately
- Added `useSearchParams` + `?selected=` param to businesses page so lead detail can deep-link to specific business
- Added ArrowLeft back-to-leads link on businesses page for navigation consistency
- For B4: Need to handle dual JSON shapes in extractors (old scrape format vs new adapter format) — `contactInfo.emails` vs `scrape.emails`, `technologies` vs `techStack`, `businessSignals.certifications` vs `scrape.certifications`
</decisions_made>

<blockers>
- None. B4 is straightforward code replacement, just incomplete.
</blockers>

<context>
This is Session B of a parallel multi-session frontend overhaul. Files owned exclusively:
- `apps/web/app/dashboard/leads/page.tsx`
- `apps/web/app/dashboard/leads/[id]/page.tsx`
- `apps/web/app/dashboard/leads/businesses/page.tsx`

The lead detail page currently has an import mismatch — removed some lucide icons used by ActivityTimeline but haven't deleted the ActivityTimeline code yet. The file will NOT typecheck until B4 is finished.

Key data shapes from the WebsiteScraperAdapter (the actual stored JSON):
- `contactInfo: { emails: [{email, context, pageUrl}], phones: [{number, type, pageUrl}], addresses: [{text, pageUrl}] }`
- `socialLinks: [{platform, url, handle}]`
- `technologies: {analytics: [], crm: [], liveChat: [], emailMarketing: [], ecommerce: [], payments: [], cssFrameworks: [], hosting: []}`
- `businessSignals: {estimatedEmployeeCount, certifications: [], hasClientLogos, hasTestimonials, hasCaseStudies}`
- `decisionMakers: [{name, title, email, linkedinUrl, seniority, source}]`

The existing extractors in the file use older field paths (scrape.techStack, scrape.certifications). Need to handle both for safety.
</context>

<next_action>
1. Delete the entire Activity Timeline section (STATUS_ORDER, statusReached, TimelineEvent, buildTimeline, ActivityTimeline component — everything between `// ── Activity Timeline` and `export default function LeadDetailPage`)
2. Rewrite CompanyIntelligence into a comprehensive IntelligenceGathered component with all sections
3. Update extractors to handle dual JSON formats
4. Replace the Activity Timeline render card with IntelligenceGathered
5. Run `pnpm typecheck && pnpm lint && pnpm build`
6. Commit
</next_action>
