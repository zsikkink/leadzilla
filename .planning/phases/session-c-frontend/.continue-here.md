---
phase: session-c-frontend
task: 1
total_tasks: 13
status: in_progress
last_updated: 2026-03-04T21:32:56.145Z
---

<current_state>
Session C — Frontend: Leads + Business Intel + Scoring. 13 tasks (C1-C13).
Currently mid-way through C1 (score simulation fix). Two of three edits complete on rules/page.tsx.
No other tasks started yet.
</current_state>

<completed_work>

- Read all 7 owned files (rules/page.tsx, leads/page.tsx, leads/[id]/page.tsx, businesses/page.tsx, dashboard/page.tsx, messages/page.tsx, api-client.ts)
- Read seed.ts to identify all 16 UNIVERSAL_RULES fieldKeys
- C1 (partial): Added 8 missing fields to SimFormState interface + DEFAULT_SIM defaults
- C1 (partial): Added 10 missing fieldKey entries to FIELD_KEY_MAP (country, data_alignment_score, industry_supported, custom_order_signals, has_booking_or_contact_form, apify_payment_widget_count, apify_has_pricing_tiers, pure_self_serve_ecom, price_led_mindset, country alias)
- C1 STILL NEEDED: Add UI form controls for the new fields (data alignment slider, business signal checkboxes) in the simulation form JSX
</completed_work>

<remaining_work>

- C1: Add UI controls for dataAlignmentScore (slider 0-1), industrySupported, customOrderSignals, hasBookingOrContactForm, paymentWidgetCount, hasPricingTiers, pureSelfServeEcom, priceLedMindset in the simulation form JSX section
- C2: Remove Source column (th+td) from leads/page.tsx table. Remove SOURCE_DISPLAY_MAP + formatSource if unused
- C3: Verify lead name displays correctly (should be automatic from Session A)
- C4: Add "Team Members" collapsible section to leads/[id]/page.tsx — query business_contacts table via Supabase, fallback to scrape JSON decisionMakers
- C5: Add Instagram recent posts subsection to leads/[id]/page.tsx — read apifyInstagramScrapeJson.recentPosts[]
- C6: Ensure ALL collected intel displayed in structured sections on lead detail (contact info, social, tech stack, business signals, website metadata)
- C7: Fix extractTechStack() in businesses/page.tsx — handle both legacy array and new object format
- C8: Fix extractContactInfo() in businesses/page.tsx — handle both legacy flat arrays and new contactInfo.emails[]/phones[] object format
- C9: Add recent Instagram posts to businesses/page.tsx detail panel
- C10: Add business_contacts team members to businesses/page.tsx detail panel
- C11: Info parity check between lead detail and business intel pages
- C12: Investigate dashboard page lead count (may be auto-fixed by Session B's B4)
- C13: Change variant.variantKey display from "variant_a" to "Message" in message-draft-card.tsx line 57
</remaining_work>

<decisions_made>

- FIELD_KEY_MAP root cause: seed rules use `country` as fieldKey but map only had `country_code`. Added both as aliases so either works.
- Added `data_alignment_score` with default 0.7 (above the 0.3 HARD_FILTER threshold so default simulation passes).
- Business signal fields (industry_supported, custom_order_signals, etc.) default to reasonable values: industrySupported=true, hasBookingOrContactForm=true, paymentWidgetCount=1, others false.
- Lead detail page ([id]/page.tsx) already has comprehensive IntelligenceGathered component with dual-format extractors for tech stack, contact info, certifications. The businesses/page.tsx extractors are OLDER and still use legacy-only format — those need fixing (C7, C8).
</decisions_made>

<blockers>
- None currently. All tasks are frontend-only within owned files.
- Note: businesses/page.tsx changes from other sessions (Session A writes business_contacts table) must be merged first for C4/C10 Supabase queries to work.
</blockers>

<context>
This is one of 4 parallel frontend sessions. File ownership is strictly separated:
- Session C owns: rules/page.tsx, leads/page.tsx, leads/[id]/page.tsx, businesses/page.tsx, dashboard/page.tsx, messages/page.tsx, api-client.ts
- Must NOT touch: packages/providers/**, apps/worker/**, apps/api/**, discovery/page.tsx, jobs/**, analytics/**

The lead detail page ([id]/page.tsx) already has well-structured dual-format extractors (extractBusinessTechStack, extractContactEmails, extractContactPhones, extractBusinessCertifications) that handle both legacy and new scraper formats. The businesses/page.tsx extractors (extractTechStack, extractContactInfo) are simpler/older and need updating to match.

The message-draft-card.tsx component is at apps/web/src/components/message-draft-card.tsx, line 57 shows `{variant.variantKey}` which needs to become "Message" for single-variant drafts.
</context>

<next_action>
Start with: Add the remaining UI form controls for C1's new SimFormState fields in rules/page.tsx (data alignment slider + business signal checkboxes), then move to C2 (remove source column — quick edit). Then tackle the larger C4-C6 and C7-C10 blocks.
</next_action>
