---
phase: post-merge-audit
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-03-05T06:28:01Z
---

<current_state>
Post-merge verification of two parallel plans completed partially. Stash conflicts resolved but not yet committed. Build is green (typecheck passes). Servers are running. User wants to verify that ALL work from both parallel plans is actually in the codebase before continuing.

Two parallel plans were executed:
1. `.agents/plans/2026-03-04-pipeline-ui-fixes-parallel.md` — 3 sessions (A, B, C) — Pipeline root cause + UI fixes
2. `.agents/plans/ui-fixes-performance-parallel.md` — 5 sessions (A, B, C, D, E) — UI fixes + performance + message personalization
</current_state>

<completed_work>

## Task 1: Compilation verification — DONE
- typecheck, lint, 137 tests, build all pass

## Task 2: Post-merge audit of plan #1 (pipeline-ui-fixes-parallel) — DONE
Findings:
- **BUG-1 FIXED**: discover/page.tsx sent N API calls per ICP instead of one `icpProfileIds` array call. Fixed in commit `17d0be1`
- **BUG-2 FIXED**: debug/page.tsx Message Send showed "skipped" instead of "pending" on enrichment failure. Fixed in commit `17d0be1`
- **AUTH FIX**: Supabase stale refresh token error — added signOut() call on getSession() failure in auth-context.tsx (uncommitted)
- Data contracts verified: leads sort (OK), analytics funnel (OK), feedback sections (OK), removal propagation (OK), singleton keys (OK), pipeline continuity (OK)

## Task 3: Stash recovery — IN PROGRESS
- Stash `stash@{0}` (from 2026-03-04 16:26) contained 15 files of discovery pipeline fixes
- Applied via `git stash pop` — 3 merge conflicts resolved:
  1. `discovery.service.ts` — dropped `extends Pick` that conflicted with optional fields
  2. `icp-category-map.ts` — merged underscore-canonical keys from stash + segment mappings from upstream
  3. `seed_tasks.ts` — took stash version (requires ICP config, Fisher-Yates sampling), kept resolveCountries helper
- Typecheck passes after conflict resolution
- **NOT YET COMMITTED** — changes are staged but not committed
</completed_work>

<remaining_work>

## Task 3 (continued): Commit stash recovery
- Run full verification (lint, test, build) on resolved stash
- Commit the stash apply + auth fix

## Task 4: Cross-reference plan #2 (ui-fixes-performance-parallel) — NOT STARTED
- Need to verify all 16 tasks (A1-A5, B1-B4, C1, D1-D4, E1-E2) are in the codebase
- The commits from that plan are in git history (9442a2c, 3ab1a6e, c8246f4, 2ea2b7f, be048b7) but subsequent sessions may have overwritten or regressed some work
- User specifically concerned that "only half" of the work is present

## Data situation
- **Real data is GONE** — Supabase was recreated, tables exist but only seed/fake data remains (13 fake leads, 0 businesses)
- Docker DB at :5434 is completely empty
- 3 stale "running" discovery runs were cleaned up (marked failed)
- User needs to run fresh discovery to populate real data once pipeline is confirmed working
</remaining_work>

<decisions_made>

- Took stash version for seed_tasks.ts (requires ICP config, removes v1 path) since v1 is deprecated
- Merged both category map approaches — underscore-canonical keys (stash) plus all 8 ICP segment mappings (upstream)
- Dropped `extends Pick<CreateDiscoveryRunRequest, ...>` from DiscoveryRunJobPayload because it conflicts with optional field redeclarations under exactOptionalPropertyTypes
- Marked 3 stale "running" discovery runs as "failed" in Supabase DB
</decisions_made>

<blockers>
- User interrupted audit of plan #2 to pause work — that cross-reference is the critical remaining task
- Real data loss means no way to verify pipeline output quality until a fresh discovery run completes
</blockers>

<context>
User has been working for 4+ hours across multiple parallel sessions. They're frustrated because:
1. The Business Intelligence page shows no businesses (data gone from Supabase recreation)
2. Leads show fake seed data with unreachable URLs
3. Discovery runs still showed 3 "running" (multi-ICP bug created duplicates)
4. They feel "only half" of their work is present

The key next step is: finish committing the stash recovery, then systematically verify every task from plan #2 (ui-fixes-performance-parallel.md) against the actual code. The commits exist in history but some may have been overwritten by later sessions.

Active git state: stash changes staged (12 files) + auth-context.tsx modified (unstaged) + untracked docs/plans files.
Servers are running: Web :3000, API :5050, Worker background, Supabase :54322.
</context>

<next_action>
1. Run `pnpm lint && pnpm test && pnpm build` to verify stash apply is clean
2. Commit: stash recovery + auth fix
3. Cross-reference all 16 tasks from `.agents/plans/ui-fixes-performance-parallel.md` against current code
4. Report which tasks are DONE vs MISSING vs REGRESSED
5. If pipeline is confirmed working, run a fresh discovery (1 ICP, limit=5, AE) to repopulate real data
</next_action>
