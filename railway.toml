# Intentionally left service-neutral.
#
# This repository deploys multiple Railway services from the same root, and the
# service-level settings already pin the correct Dockerfile path and healthcheck
# per service. Keeping repo-level Railway config blank avoids forcing API-only
# settings onto the worker during GitHub-connected deploys.
